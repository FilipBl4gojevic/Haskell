-- Create a function hasPath that determines if a path from
-- one node to another exists within a directed graph (check the video
-- for the graph explanation)

hasPath :: [(Int,Int)] -> Int -> Int -> Bool
